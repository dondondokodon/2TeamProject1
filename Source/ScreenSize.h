#pragma once
#include <windows.h>

extern const LONG SCREEN_WIDTH = 1280;
extern const LONG SCREEN_HEIGHT = 720;

extern LONG SCREEN_W;//サイズが変動する（フルスクリーンとかで変わったときもそっちに合う）
extern LONG SCREEN_H;//サイズが変動する（フルスクリーンとかで変わったときもそっちに合う）